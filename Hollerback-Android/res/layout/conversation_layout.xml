<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_gravity="center"
    android:layout_weight="1"
    android:animateLayoutChanges="true"
    android:background="@color/background_header"
    android:clipChildren="false"
    android:orientation="vertical" >

    <VideoView
        android:id="@+id/vv_preview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <ProgressBar
        android:id="@+id/progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="visible" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:background="@android:color/transparent"
        android:visibility="gone" >

        <ImageButton
            android:id="@+id/ib_skip_backward"
            android:layout_width="@dimen/dim_46dp"
            android:layout_height="@dimen/dim_46dp"
            android:layout_marginRight="@dimen/dim_10dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_skip_backward" />

        <ImageButton
            android:id="@+id/ib_skip_forward"
            android:layout_width="@dimen/dim_46dp"
            android:layout_height="@dimen/dim_46dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_skip_forward" />
    </LinearLayout>

</FrameLayout>